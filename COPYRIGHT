Sendsprite — self-hosted email API on Amazon SES
Copyright (C) 2026 defy.works

Everything in this repository is MIT licensed — see LICENSE.md at the root.
The published client packages carry their own copy of the same licence so the
text ships inside the npm tarballs:

  packages/sdk    ->  the `sendsprite` npm package (SDK, /react, /next, CLI)
  packages/mcp    ->  the `@sendsprite/mcp` npm package
  packages/shared ->  bundled *into* the packages above

The "Sendsprite" name and logo are trademarks and are not covered by the
licence — see TRADEMARK.md.
